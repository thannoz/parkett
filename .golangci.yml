version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
    - copyloopvar
    - nolintlint
  settings:
    misspell:
      locale: US

formatters:
  enable:
    - gofumpt
    - goimports
